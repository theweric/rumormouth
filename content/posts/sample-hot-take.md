---
title: "The award-show red carpet isn't dead, it just moved to the after-party"
type: original
byline: "The Desk"
date: 2026-07-07
category: "Red Carpet"
excerpt: "An argument for why the real fashion story of the season has been happening after the cameras officially stopped rolling."
---

This is placeholder body copy — replace with the real article before launch.

Original posts live as Markdown files in `content/posts/`. Each one has:

- `type: original` — a staff-written Hot Take, published in full on this site
- `type: wire` — a curated item pulled from an external feed (see `scripts/fetch-feed.js`) — these should almost always be **excerpt + link back**, never the full external article
- `type: release` — a press release republished in full, because the issuing wire service licenses it for that purpose

Keep the frontmatter fields consistent (`title`, `type`, `byline`, `date`, `category`, `excerpt`) so a build script can loop over this folder and generate pages/listings automatically later.
